Run one bounded research iteration in ${TOPIC_DIR}.

1. Read ${CHASSIS}/CONTRACT-CORE.md, TOPIC.md, and AUTHORITY.md directly. Run `python3 ${CHASSIS}/semantic-state.py select ${TOPIC_DIR}`; use `... get ${TOPIC_DIR} <id>` for needed full records — never read SEMANTIC-STATE.json whole. Read current operator/proposal decisions (DECISIONS-LOG.md), pending evidence, and relevant synthesis, contradiction, and attempt records. Bound history reads by relevance and size without omitting binding decisions. TOPIC.md and AUTHORITY.md are operator-owned; never rewrite their scope, obligations, process, quality dimensions, deliverables, dependencies, or exit condition. Inherited completion marks are historical evidence, not current authority.

2. Reconcile pending evidence first. Follow the queue/operator-assigned iteration type. For ordinary research, apply the topic's work-selection and dependency rules to choose one highest-value feasible action on approved work (required contradictions and deliverables included); prefer an actionable open obligation, and when open work is blocked, identify the blocker and consider other approved work that can advance — terminal status alone never means the contract is covered. For deepening, name the weakest material assessment with a concrete remaining evidence action that could change the answer, confidence, boundary, or adequate-search assessment; state the selected vulnerability and next action in one sentence; do not select by rotation or source count alone.

3. Plan and group independent lookups for the selected work. Reconcile source identities and prior attempts before discovery, reusing an adequate librarian packet. Prefer extraction packets for acquisition; read the load-bearing passages and needed context. Compare plausible overlapping sources before substantive exclusion. Follow docs/research-workflow.md for bounded packets, triage rows, and lookup batching. Independently verify fixed claims and locations before accepting their support; temporary access failure is not hallucination or evidence absence.${AGENT_NOTE}${CITATION_NOTE}${DEGRADED_NOTE}

4. During ordinary work, preserve at most one short, evidence-anchored question signal when warranted; route covered questions to existing obligations. An empty deepening result does not launch a scout debate. For an assigned checkpoint or a ready proposal, read docs/obligations-checkpoint.md and use its admission, allowance, and episode limits: at most three cards, one fresh Terra counter pass, and at most one permitted repair exchange. The primary writes final proposals within the remaining allowance. Scout/checkpoint proposals and all amendments require operator promotion, regardless of gap_policy. Do not research an unpromoted scout question. Review prose and pending proposals are neither scientific evidence nor qualifying deepening.${GAP_POLICY_NOTE}

5. Integrate verified results under the topic's quality rules. Use only the state CLI — `python3 ${CHASSIS}/semantic-state.py transition ${TOPIC_DIR} <id> ...` for obligation dispositions/fields, `... pending ${TOPIC_DIR} --add/--remove <ref>` for pending evidence, `... deliverable ${TOPIC_DIR} <id> ...` and `... contradiction ${TOPIC_DIR} --open/--resolve ...` for the rest (run each with --help for flags) — never rewrite SEMANTIC-STATE.json directly or by script: the CLI validates every terminal transition with the DONE gate's own rules at write time and refuses incomplete ones atomically. Update genuine changes in assessment (confidence in either direction), evidence, search, pending items, contradictions, or deliverables; leave each semantically unchanged field byte-stable and put activity narratives in the logs. Preserve counterevidence, provenance, and uncertainty. Every terminal obligation requires nonempty acceptance_summary and counterevidence_summary fields plus existing topic-local evidence_refs when supported or contradicted. Every complete deliverable requires acceptance_summary and existing topic-local acceptance_evidence_refs. Every obligation must end supported, contradicted, unresolved after an adequate recorded published-evidence search, or deferred as a precise experiment/direct-research question. A valid unchanged deepening result is useful to the queue's saturation assessment; never manufacture progress. Capability or quota failure is an attention state, never evidence absence or completion. No fixed iteration, token, source, inactivity, or revision count defines DONE.

Measurement (skip silently if $RESEARCH_LOOP_PHASE_LOG is unset): when you BEGIN each working phase, append the documented marker line (see docs/research-workflow.md) with <phase> one of setup|discovery|extraction|verification|integration. Markers are timing only; they never substitute for the state CLI and never indicate progress or iteration type.

END-OF-ITERATION PROTOCOL — run these steps, in order, as your LAST actions:
1. Join delegates or record and terminate failed work first, then run: `python3 ${CHASSIS}/semantic-state.py validate ${TOPIC_DIR}`
   Passing means the contract is COVERED; the queue alone determines completion. Writing `DONE` yourself has no effect — the queue discards it and reschedules you.
2. Write STOP NEEDS-OPERATOR only when unfinished approved work is blocked by a specific decision or action that requires the operator and no other approved work can advance — state that decision or action precisely:
   `printf 'NEEDS-OPERATOR\nflag: <precise operator question>\n' > ${TOPIC_DIR}/STOP`
   This is a shell command you execute; only the file counts. A valid unchanged deepening pass, no admissible new question, or optional proposals awaiting promotion is not by itself an operator blocker — in those cases finish normally; the queue owns completion. Never write STOP DONE.
3. Finish your reply with compact JSON naming the iteration type, selected work, verified findings, counterevidence, real state changes, next gap or action, blockers, and whether actual research made qualifying semantic progress (checkpoint/proposal work reported separately — the queue determines saturation eligibility).
In every other case, skip step 2 and just do step 3 — the queue reschedules you.
