Run exactly one bounded iteration of the research topic in ${TOPIC_DIR}. Read ${CHASSIS}/CONTRACT-CORE.md, then TOPIC.md, AUTHORITY.md, the semantic-state work-selection view (`python3 ${CHASSIS}/semantic-state.py select ${TOPIC_DIR}` — never read SEMANTIC-STATE.json whole; fetch any single full record you need with `... get ${TOPIC_DIR} <id>`), DECISIONS-LOG.md, pending evidence, the tail of PROGRESS.md, and relevant synthesis sections. TOPIC.md and AUTHORITY.md are operator-owned; never rewrite their scope, obligations, process, quality dimensions, deliverables, dependencies, or exit condition. If you find a real gap not covered by any existing obligation, propose one via a PROPOSAL row in DECISIONS-LOG.md rather than silently expanding scope.${GAP_POLICY_NOTE} Migration-era or inherited unit taxonomies and completion marks are historical evidence only. Select one highest-value unblocked open obligation from the `select` view after reviewing pending evidence first. If NO open obligations remain, you are in the DEEPENING PHASE, not done: select the WEAKEST terminal obligation — single-source or mixed-support confidence, thinnest evidence base, or unchased leads logged in NEEDS-SOURCE.md/PROGRESS.md — and deepen it this iteration: chase the logged leads, search lanes not yet tried, independently verify secondary-report or abstract-only citations against full text, add verified evidence or elevate confidence through the state CLI. Weakest means weakest, not least recently touched: rotation is not selection. If a deepening pass finds NOTHING left to deepen, its job becomes OBLIGATION SCOUTING, not narrating emptiness: reason over this topic's own synthesis, findings, contradictions, and dead ends and ask what this corpus now makes askable that the original obligations could not have anticipated — then hold a DEBATE before any PROPOSAL is written. Run the ADVOCATE delegate (the named secondary's default model) to draft candidate next questions with their corpus-cited reasoning, and the COUNTER-ARGUMENT delegate (the same wrapper with --model claude-haiku-4-5 — a different model family, deliberately) to argue the other side of the topic's UNDERLYING question: which candidates are rephrasings of exhausted residuals or speculation, what the corpus says the research should pursue INSTEAD, and what direction the advocate missed. Exchange each side's latest output for THREE rounds per surviving point (a fresh delegate invocation per round), recording the full exchange verbatim to logs/scout-debate-<stamp>.md. Then YOU read the whole debate, reason through it, and write the final PROPOSAL rows in DECISIONS-LOG.md — adopting, merging, or rejecting each debated point WITH your reasons and a reference to the debate file; each proposal still carries the findings that motivate it (cited), why it is inside this topic's mission, what answering it would change, and why it looks answerable. The debate never writes ledgers, state, or proposals itself. Research is allowed to redirect the questions; only the operator may ACCEPT the redirection — never promote your own proposal, and a pass that proposes nothing records why the corpus suggests no new question yet, counter-argument heard. Saturation, not coverage, defines done: the queue completes the topic only after several consecutive deepening passes change nothing, so an unproductive verification pass wastes an iteration — deepen or record precisely why nothing remained to deepen. Preserve discovery/extraction separation, independently verify load-bearing claims, actively seek counterevidence, preserve contradictions, deduplicate evidence, and retain provenance and temporal metadata. Plan the selected obligation's lookups FIRST, then execute them GROUPED: research_batch carries up to 20 independent resolve/enrich lookups in one call; use a source's native multi-item form where it exists (one research_data call carries up to 50 BLS series); reuse identifiers, continuation cursors and sources already in this topic's ledgers before re-discovering them (a citation-index hit is a lead — the original citation still gets examined, and freshness still gets checked); discover parameters through research_sources/research_catalog instead of guessing. One obligation per iteration never means one query per iteration. Run the LIBRARIAN delegate pass before new discovery (reconcile candidate DOIs/URLs/titles against SOURCE-LEDGER.md/NEEDS-SOURCE.md, surface prior failed routes and stale leads) and prefer EXTRACTION packets for acquisition — you read the load-bearing passages and judge; you do not re-run the mechanical fetching. Overlap alone never discards a source: compare an overlapping candidate (stronger? counterpoint? opens gaps?) and cite it or record the comparison; never reject a candidate on a delegate's characterization without examining it or its extracted passages.${AGENT_NOTE}${CITATION_NOTE} A source is qualifying progress only if it changes a named obligation disposition/confidence/gap, contradiction, required comparison, or deliverable; source volume and prose churn do not count. Semantic fields carry the CURRENT assessment only — rewrite them when belief changes, leave them byte-unchanged when it does not (pass narratives go to PROGRESS.md/NEEDS-SOURCE.md; unchanged belief leaving state untouched IS the saturation signal). Record those semantic changes ONLY through the state CLI — `python3 ${CHASSIS}/semantic-state.py transition ${TOPIC_DIR} <id> ...` for obligation dispositions/fields, `... pending ${TOPIC_DIR} --add/--remove <ref>` for pending evidence, `... deliverable ${TOPIC_DIR} <id> ...` and `... contradiction ${TOPIC_DIR} --open/--resolve ...` for the rest (run each with --help for flags) — using the topic's own quality and confidence rules. Never rewrite SEMANTIC-STATE.json directly or by script: the CLI validates every terminal transition with the DONE gate's own rules at write time and refuses incomplete ones atomically. Every terminal obligation requires nonempty acceptance_summary and counterevidence_summary fields plus existing topic-local evidence_refs when supported or contradicted. Every complete deliverable requires acceptance_summary and existing topic-local acceptance_evidence_refs. Every obligation must end supported, contradicted, unresolved after an adequate recorded published-evidence search, or deferred as a precise experiment/direct-research question. Capability or quota failure is an attention state, never evidence absence or completion.${DEGRADED_NOTE} No fixed iteration, token, source, inactivity, or revision count defines DONE.

Measurement (skip silently if $RESEARCH_LOOP_PHASE_LOG is unset): when you BEGIN each working phase, append one marker line — `echo "$(date -u +%FT%TZ) <phase>" >> $RESEARCH_LOOP_PHASE_LOG` — with <phase> one of setup|discovery|extraction|verification|integration. Markers are timing only; they never substitute for the state CLI.

END-OF-ITERATION PROTOCOL — run these steps, in order, as your LAST actions:
1. Run: `python3 ${CHASSIS}/semantic-state.py validate ${TOPIC_DIR}`
   This tells you whether the contract is COVERED. It does not mean you are done, and you cannot declare yourself done: the queue completes a topic only after consecutive deepening passes stop changing its semantic signature. Writing `DONE` yourself has no effect — the queue discards it and reschedules you. Keep deepening; the gate will notice.
2. If (and only if) no unblocked obligation can advance without the operator, run:
   `printf 'NEEDS-OPERATOR\nflag: <what to look at>\n' > ${TOPIC_DIR}/STOP`
   This is a shell command you execute. Typing the words in your reply does nothing — only the file counts. Being blocked is the one terminal signal that is yours to send.
3. Finish your reply with compact JSON naming the obligation, verified findings, counterevidence, state changes, next gap, and whether this was qualifying semantic progress.
In every other case, skip step 2 and just do step 3 — the queue reschedules you.
