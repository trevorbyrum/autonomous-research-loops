Run exactly one bounded iteration of the research topic in ${TOPIC_DIR}. Read ${CHASSIS}/CONTRACT-CORE.md, then TOPIC.md, AUTHORITY.md, the semantic-state work-selection view (`python3 ${CHASSIS}/semantic-state.py select ${TOPIC_DIR}` — never read SEMANTIC-STATE.json whole; fetch any single full record you need with `... get ${TOPIC_DIR} <id>`), DECISIONS-LOG.md, pending evidence, the tail of PROGRESS.md, and relevant synthesis sections. TOPIC.md and AUTHORITY.md are operator-owned; never rewrite their scope, obligations, process, quality dimensions, deliverables, dependencies, or exit condition. If you find a real gap not covered by any existing obligation, propose one via a PROPOSAL row in DECISIONS-LOG.md rather than silently expanding scope.${GAP_POLICY_NOTE} Migration-era or inherited unit taxonomies and completion marks are historical evidence only. Reconcile pending evidence first, then apply the topic's work-selection and dependency rules to the complete `select` view and choose the highest-value feasible next action on approved work (required contradictions and deliverables included); prefer an actionable open obligation, and when open work is blocked, identify the blocker and consider other approved work that can advance — terminal status alone never means the contract is covered. For deepening, choose the weakest material assessment for which a concrete, uncompleted evidence action could change the answer, confidence, boundary, or adequate-search assessment (the topic's quality dimensions govern; source count and recency are clues, not ranking rules); state the selected vulnerability and next action in one sentence; never rotate by last-touch time or repeatedly re-choose an unworkable residual. Saturation, not coverage, defines done: a valid deepening pass may change an assessment, lower confidence, or leave it unchanged — record the specific research performed and its result; a justified unchanged result is useful to the queue's saturation assessment, while repeated verification without a changed claim, location, evidence need, or freshness requirement is avoidable work, and inventing state changes to appear productive is prohibited. Integrate the verified result and revise the assessment in either direction when warranted. Preserve discovery/extraction separation, independently verify load-bearing claims, actively seek counterevidence, preserve contradictions, deduplicate evidence, and retain provenance and temporal metadata.${AGENT_NOTE}${CITATION_NOTE} A source is qualifying progress only if it changes a named obligation disposition/confidence/gap, contradiction, required comparison, or deliverable; source volume and prose churn do not count. Record those semantic changes ONLY through the state CLI — `python3 ${CHASSIS}/semantic-state.py transition ${TOPIC_DIR} <id> ...` for obligation dispositions/fields, `... pending ${TOPIC_DIR} --add/--remove <ref>` for pending evidence, `... deliverable ${TOPIC_DIR} <id> ...` and `... contradiction ${TOPIC_DIR} --open/--resolve ...` for the rest (run each with --help for flags) — using the topic's own quality and confidence rules. Never rewrite SEMANTIC-STATE.json directly or by script: the CLI validates every terminal transition with the DONE gate's own rules at write time and refuses incomplete ones atomically. Every terminal obligation requires nonempty acceptance_summary and counterevidence_summary fields plus existing topic-local evidence_refs when supported or contradicted. Every complete deliverable requires acceptance_summary and existing topic-local acceptance_evidence_refs. Every obligation must end supported, contradicted, unresolved after an adequate recorded published-evidence search, or deferred as a precise experiment/direct-research question. Capability or quota failure is an attention state, never evidence absence or completion.${DEGRADED_NOTE} No fixed iteration, token, source, inactivity, or revision count defines DONE.

END-OF-ITERATION PROTOCOL — run these steps, in order, as your LAST actions:
1. Run: `python3 ${CHASSIS}/semantic-state.py validate ${TOPIC_DIR}`
   This tells you whether the contract is COVERED. It does not mean you are done, and you cannot declare yourself done: the queue completes a topic only after consecutive deepening passes stop changing its semantic signature. Writing `DONE` yourself has no effect — the queue discards it and reschedules you. Keep deepening; the gate will notice.
2. Write STOP NEEDS-OPERATOR only when unfinished approved work is blocked by a specific decision or action that requires the operator and no other approved work can advance — state that decision or action precisely:
   `printf 'NEEDS-OPERATOR\nflag: <precise operator question>\n' > ${TOPIC_DIR}/STOP`
   This is a shell command you execute; only the file counts. A valid unchanged deepening pass, no admissible new question, or optional proposals awaiting promotion is not by itself an operator blocker — in those cases finish normally; the queue owns completion. Never write STOP DONE.
3. Finish your reply with compact JSON naming the obligation, verified findings, counterevidence, state changes, next gap, and whether this was qualifying semantic progress.
In every other case, skip step 2 and just do step 3 — the queue reschedules you.
